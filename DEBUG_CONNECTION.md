# 串口连接失败诊断步骤

## 问题：Command timeout（命令超时）

### 步骤 1：检查控制台窗口

双击运行 `dist/web-serial-terminal.exe` 后，控制台窗口应该显示：

```
╔════════════════════════════════════════════╗
║  Web Serial Terminal <version>             ║
╚════════════════════════════════════════════╝

  🌐 Web Interface: http://localhost:8080
  ...
```

**请检查：**
- 控制台窗口是否打开了？
- 是否有任何错误信息？
- 点击连接时，是否有新的日志输出？

### 步骤 2：检查浏览器控制台

按 F12 打开浏览器开发者工具，检查：

**Console 标签：**
- 是否有 WebSocket 相关错误？
- 是否有其他 JavaScript 错误？

**Network 标签：**
- 是否有 WebSocket 连接（ws://localhost:8080/ws）？
- WebSocket 状态是否为 101（成功）？

### 步骤 3：检查端口状态

1. 打开设备管理器（Win+X → M）
2. 展开"端口 (COM 和 LPT)"
3. 找到你的 CH340 设备
4. 右键 → 属性 → 查看端口设置

**确认：**
- 端口名称（COM几）
- 是否有黄色感叹号（驱动问题）

### 步骤 4：尝试其他端口

如果可能，尝试：
1. 插拔 USB 设备
2. 换一个 USB 接口
3. 重启电脑
4. 禁用"自动打开浏览器"后手动测试

### 步骤 5：简化测试

运行以下命令手动测试后端：

```batch
cd backend
go run . --auto-open=false
```

然后在浏览器访问 http://localhost:8080，查看是否正常。

---

## 常见问题

### Q: WebSocket 连接失败
**A:**
- 检查后端是否启动（控制台窗口是否打开）
- 尝试刷新页面
- 检查防火墙是否阻止连接

### Q: 端口一直被占用
**A:**
- 运行 `kill.bat` 关闭所有实例
- 关闭所有串口工具
- 重启电脑

### Q: 找不到端口
**A:**
- 检查设备管理器
- 安装/更新 CH340 驱动
- 换 USB 接口

---

## 需要提供的信息

如果以上步骤都无法解决，请提供：

1. **控制台窗口的完整输出**（截图或文字）
2. **浏览器控制台的错误信息**（F12 → Console）
3. **设备管理器中的端口名称**
4. **连接时选择的波特率**

这样我才能准确定位问题！