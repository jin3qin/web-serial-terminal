# 待实现功能清单

> 最后更新：2026-08-07

## 第二优先级（用户体验）

### 1. 自定义宏/快捷键（P2-1）

**状态**：✅ **已完成**

**功能描述**：
- 可编辑按键名称、发送内容（hex/ascii）
- 鼠标悬停显示发送的具体内容
- 单击回填，双击发送
- 支持添加、编辑、删除
- 宏列表持久化到 localStorage

**涉及文件**：
- `src/types/serial.ts` - MacroShortcut 类型定义 ✅
- `src/utils/storage.ts` - 宏持久化 ✅
- `src/components/send/MacroPanel.tsx` - 宏面板组件 ✅

**实现特性**：
- 宏上限 20 条
- 支持文本/HEX 两种发送模式
- 悬停提示显示描述和发送内容
- 编辑对话框支持新增/编辑
- 删除确认
- 自动持久化到 localStorage

**验收标准**：
1. 添加新宏，刷新页面后宏仍在 ✅
2. 编辑宏名称和内容，保存后生效 ✅
3. 鼠标悬停显示完整的发送内容 ✅
4. 单击回填到输入框，双击直接发送 ✅

---

### 2. 快捷指令分组（P2-2）

**状态**：✅ **已完成**

**功能描述**：
- 支持创建多个分组（最多10个）
- 快捷指令可关联到分组
- 分组可折叠/展开
- 支持分组的增删改
- 自动迁移旧版本数据

**涉及文件**：
- `src/types/serial.ts` - MacroGroup、MacroStorage 类型定义 ✅
- `src/utils/storage.ts` - 分组存储与迁移逻辑 ✅
- `src/components/send/MacroPanel.tsx` - 分组UI组件 ✅

**实现特性**：
- 分组上限 10 个
- 快捷指令上限提升至 50 个
- 默认提供"AT 指令"和"Modbus"分组
- 分组折叠状态本地记忆
- 未分组快捷指令单独展示
- 编辑快捷指令时可选择所属分组
- 数据迁移：旧版本数据自动迁移到新格式

**验收标准**：
1. 创建新分组，刷新后分组仍在 ✅
2. 快捷指令关联到分组，修改所属分组 ✅
3. 折叠/展开分组，状态持久化 ✅
4. 删除分组，组内快捷指令变为未分组 ✅
5. 旧版本数据自动迁移，无数据丢失 ✅

**功能描述**：
- 可编辑按键名称、发送内容（hex/ascii）
- 鼠标悬停显示发送的具体内容
- 单击回填，双击发送
- 支持添加、编辑、删除
- 宏列表持久化到 localStorage

**涉及文件**：
- `src/types/serial.ts` - MacroShortcut 类型定义 ✅
- `src/utils/storage.ts` - 宏持久化 ✅
- `src/components/send/MacroPanel.tsx` - 宏面板组件 ✅

**实现特性**：
- 宏上限 20 条
- 支持文本/HEX 两种发送模式
- 悬停提示显示描述和发送内容
- 编辑对话框支持新增/编辑
- 删除确认
- 自动持久化到 localStorage

**验收标准**：
1. 添加新宏，刷新页面后宏仍在 ✅
2. 编辑宏名称和内容，保存后生效 ✅
3. 鼠标悬停显示完整的发送内容 ✅
4. 单击回填到输入框，双击直接发送 ✅

---

## 第一优先级（核心增强）

### 1. 配置持久化（P1-6）

**状态**：✅ **已完成**

**功能描述**：
- 保存连接参数、发送选项、显示选项到 localStorage
- 刷新页面后自动恢复配置
- 支持配置版本迁移（避免兼容性问题）

**涉及文件**：
- `src/utils/storage.ts` - localStorage 封装与版本迁移 ✅
- `src/hooks/useSerialConnection.ts` - 连接时保存配置 ✅
- `src/types/serial.ts` - `PersistedProfile` 类型定义 ✅（已存在）

**实现特性**：
- localStorage 不可用时降级为内存存储（隐私模式兼容）
- 配置校验与补全（防止损坏数据导致崩溃）
- 版本迁移机制（PROFILE_VERSION）
- 连接成功、断开连接时自动保存
- 应用启动时自动恢复配置

**验收标准**：
1. 修改波特率后刷新页面，波特率保持修改后的值 ✅
2. 切换 HEX 显示模式后刷新，仍为 HEX 模式 ✅
3. localStorage 损坏时不崩溃，回退默认值 ✅

---

### 2. 发送历史（P1-2）

**状态**：✅ **已完成**

**功能描述**：
- 记录最近 50 条发送内容（去重，最新在前）
- 点击历史项回填到输入框
- 双击历史项直接发送
- 清空历史按钮
- 历史记录持久化到 localStorage

**涉及文件**：
- `src/components/send/SendHistoryList.tsx` - 历史列表组件 ✅
- `src/store/useMessageStore.ts` - history 数组与 pushHistory 方法 ✅
- `src/utils/storage.ts` - 历史持久化 ✅
- `src/hooks/useSerialConnection.ts` - 发送时添加历史 ✅

**实现特性**：
- 发送成功后自动添加到历史（最新在前）
- 去重机制（相同内容自动提升到首位）
- 上限 50 条，超出丢弃最旧
- 单击回填、双击直发
- 删除单条、清空全部
- 可折叠展开
- 自动持久化到 localStorage

**验收标准**：
1. 发送 10 条内容，历史列表显示 10 条（最新在上） ✅
2. 点击历史项，输入框被填充 ✅
3. 双击历史项，立即发送 ✅
4. 刷新页面后历史仍在 ✅

---

## 第三优先级（高级功能）

### 5. RTS/DTR 信号线控制（P2-5）

**状态**：✅ **已完成**

**功能描述**：
- RTS/DTR 开关控件（可写）
- CTS/DSR/DCD/RI 信号线状态显示（只读）
- 收纳在「高级」折叠区
- 用于 ESP32/STM32 一键下载复位

**涉及文件**：
- `src/components/toolbar/SignalControl.tsx` - 信号线控制组件 ✅
- `src/components/toolbar/ConnectionToolbar.tsx` - 高级区折叠开关 ✅
- `src/components/layout/AppLayout.tsx` - 集成 SignalControl ✅
- `src/hooks/useSerialConnection.ts` - setSignals / pollInputSignals ✅

**实现特性**：
- DTR / RTS 开关（可写）
- 一键复位按钮（一键下载时序：DTR=false + RTS=true → 延时 → RTS=false）
- 输入信号线实时显示（CTS / DSR / DCD / RI，只读）
- 连接期间每秒轮询输入信号线
- 收纳在「高级」折叠区

**验收标准**：
1. RTS 开关点击后，物理引脚电平改变 ✅
2. DTR 开关点击后，物理引脚电平改变 ✅
3. 只读信号线实时显示当前状态 ✅

---



## 架构优化项

### 消息环形缓冲

**状态**：✅ **已完成**

**功能描述**：
- 消息列表上限 5000 条
- 超限后丢弃最旧的消息
- 避免长时间运行导致内存泄漏

**涉及文件**：
- `src/store/useMessageStore.ts` - append 方法改造 ✅

**实现特性**：
- `maxRecords` 字段存储上限（默认 5000）
- `trim()` 函数超出上限时丢弃最旧消息
- append / appendMany / appendSystem 均调用 trim()
- 防止长时间运行导致内存泄漏

**验收标准**：
1. 连续接收 6000 条消息后，messages.length === 5000 ✅
2. 保留的是最新的 5000 条 ✅

---

### 心跳机制优化

**状态**：✅ **已完成**

**功能描述**：
- 调整心跳超时阈值（当前 10s 可能不够）
- 优化重连逻辑（避免频繁重连）
- 添加心跳失败计数器（3 次失败才重连）

**涉及文件**：
- `src/serial/WebSocketClient.ts` - 心跳逻辑改造 ✅

**实现特性**：
- 心跳超时阈值调整为 15s（从 10s 提升到 15s）
- 心跳失败计数器（连续 3 次失败才触发重连）
- 重连间隔递增（指数退避：2s → 4s → 8s → 8s...）
- 最大重连间隔限制为 8s

**验收标准**：
1. 心跳超时阈值调整为 15s ✅
2. 单次心跳失败不立即重连，3 次失败才重连 ✅
3. 重连间隔递增（2s → 4s → 8s） ✅

---